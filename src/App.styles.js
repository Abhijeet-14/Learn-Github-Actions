import { css, StyleSheet } from "aphrodite";

const styles = StyleSheet.create({
  app: {
    margin: "0",
    padding: "15rem",
    textAlign: "center",
    backgroundColor: "#231",
    fontSize: "5rem",
    fontStyle: "italic",
  },
});

export { css, styles };
